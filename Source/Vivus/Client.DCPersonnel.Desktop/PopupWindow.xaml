<Window x:Class="Vivus.Client.DCPersonnel.Desktop.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Vivus.Client.DCPersonnel.Desktop"
        xmlns:valueConverters="clr-namespace:Vivus.Client.DCPersonnel.Desktop.ValueConverters"
        WindowStartupLocation="CenterOwner"
        Icon="Icon.ico"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        KeyboardNavigation.IsTabStop="False"
        Title="PopupWindow" Height="450" Width="800">

    <Border x:Name="Container" Padding="74 40">
        <Border.Background>
            <SolidColorBrush Color="Black" Opacity="0.25" />
        </Border.Background>

        <Border CornerRadius="4" Background="#fafafa" MaxWidth="{Binding ElementName=Frame, Path=MaxWidth}" MaxHeight="{Binding ElementName=Frame, Path=MaxHeight}">
            <Frame x:Name="Frame" Content="{Binding CurrentPage, Converter={valueConverters:ApplicationPageToPageValueConverter}}" KeyboardNavigation.IsTabStop="False" />
        </Border>
    </Border>
</Window>
